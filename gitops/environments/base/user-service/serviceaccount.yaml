apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service
  labels:
    app: user-service
# imagePullSecrets will be added via sealed-secret or external-secrets
# Uncomment when you create the secret:
# imagePullSecrets:
#   - name: ghcr-pull-secret
